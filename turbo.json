{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": [
        "^build"
      ],
      "outputs": [
        "website/.next/**",
        "generate/data/**"
      ]
    },
    "dev": {
      "cache": false
    }
  }
}
